<UserControl x:Class="MusicMediaPlayer.UserControlMusic.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicMediaPlayer.UserControlMusic"
             mc:Ignorable="d" 
             d:DesignHeight="680" d:DesignWidth="50"
             >
    <Grid>
        <Grid>
            <Border CornerRadius="20" Grid.RowSpan="2" Opacity="1">
                <Border.Background>
                    <LinearGradientBrush>
                        <GradientStop Color="#2d388e" Offset="0"></GradientStop>
                        <GradientStop Color="#00aeef" Offset="1"></GradientStop>
                        <!--<GradientStop Color="#2af598" Offset="0.2"/>
                        <GradientStop Color="#009efd" Offset="1"/>-->
                    </LinearGradientBrush>
                </Border.Background>
             </Border>
            <StackPanel Orientation="Vertical">
                <Button Width="50"
                        Height="50"
                        Style="{StaticResource NormalButton}">
                    <Image Source="/Image/logomusicapp.png"></Image>
                </Button>
                <TextBlock Text="Music Media Player"
                           FontWeight="ExtraBlack"
                           Opacity="0.8"
                           FontSize="6"
                           Margin="0,10,0,0"
                           FontFamily="Candara"
                           Height="70"
                           >
                    <TextBlock.Foreground>
                        <LinearGradientBrush>
                            <GradientStop Color="#2af598" Offset="0.2"/>
                            <GradientStop Color="#009efd" Offset="1"/>
                        </LinearGradientBrush>
                    </TextBlock.Foreground>
                </TextBlock>
                <Button x:Name="CloseWindow"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        Style="{StaticResource NormalButton}"
                        Height="50"
                        Width="50"
                        Margin="0,520,0,0"
                        Click="Close_Click">
                    <Image Source="/Image/poweroff2.png">
                    </Image>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
